// Active Directory pen-testing — senior interview questions.
// Authority sources: MITRE ATT&CK, Microsoft Learn (Kerberos / NTLM / ADCS), SpecterOps research.

window.QUESTION_BANK.ad = [
  {
    id: 'ad-001',
    topic: 'ad',
    subtopic: 'kerberoasting',
    difficulty: 'senior',
    question: 'You hold credentials for any unprivileged domain user. Which AD configuration condition is REQUIRED for a Kerberoasting attack to be possible against a target service account?',
    choices: [
      'The target service account must have the "Account is sensitive and cannot be delegated" flag enabled',
      'The target service account must have a Service Principal Name (SPN) registered',
      'The target service account must be a member of Domain Admins',
      'The target host must have SMB signing disabled',
    ],
    correctIndex: 1,
    explanation: 'Kerberoasting works because any authenticated user can request a TGS for any account that has an SPN registered. The TGS is encrypted with the target account\'s NTLM hash (or AES key derived from its password), so an offline crack reveals the password. No SPN means no Kerberoastable target. MITRE catalogues this as T1558.003.',
    distractorRationale: {
      0: 'The "sensitive, cannot be delegated" flag prevents delegation attacks; it has no effect on whether the account is Kerberoastable.',
      2: 'Domain Admins membership is what makes the cracked credential valuable, not what makes it Kerberoastable. Any SPN-bearing account is targetable.',
      3: 'SMB signing concerns relay/MITM on SMB. Kerberoasting is an offline cracking attack against the TGS — it does not touch SMB.',
    },
    source: { name: 'MITRE ATT&CK T1558.003 — Steal or Forge Kerberos Tickets: Kerberoasting', url: 'https://attack.mitre.org/techniques/T1558/003/' },
  },

  {
    id: 'ad-002',
    topic: 'ad',
    subtopic: 'asreproast',
    difficulty: 'senior',
    question: 'AS-REP Roasting differs from Kerberoasting in one specific prerequisite. What is it?',
    choices: [
      'The target user must have "Do not require Kerberos pre-authentication" enabled',
      'The attacker must have local admin on a domain controller',
      'The target user must be a Domain Admin',
      'The domain functional level must be 2003 or below',
    ],
    correctIndex: 0,
    explanation: 'AS-REP Roasting requests an AS-REP for a user; if Kerberos pre-authentication is disabled (DONT_REQ_PREAUTH flag, UAC bit 0x400000), the KDC returns the encrypted AS-REP without verifying the requester. That ciphertext is offline-crackable. Microsoft\'s Kerberos docs and MITRE T1558.004 describe this. Crucially, no domain credentials are required — only the username.',
    distractorRationale: {
      1: 'Local admin on a DC would make this irrelevant — you would already have far more powerful options like DCSync.',
      2: 'AS-REP roasting works against any user with the flag set; admin status is not required.',
      3: 'Domain functional level affects feature availability but does not enable or disable AS-REP roasting; the per-account flag does.',
    },
    source: { name: 'MITRE ATT&CK T1558.004 — AS-REP Roasting', url: 'https://attack.mitre.org/techniques/T1558/004/' },
  },

  {
    id: 'ad-003',
    topic: 'ad',
    subtopic: 'ntlm-relay',
    difficulty: 'senior',
    question: 'You captured an NTLM authentication via LLMNR poisoning and want to relay it to an SMB server. Which target-side control most directly defeats SMB relay?',
    choices: [
      'Disabling NTLMv1 only',
      'Enabling SMB signing (server-side requirement)',
      'Disabling IPv6',
      'Enforcing minimum password length',
    ],
    correctIndex: 1,
    explanation: 'SMB signing cryptographically binds the NTLM session to the original requester. When the server requires signing, a relayed authentication cannot complete because the relayed session does not have the original session key. Microsoft\'s NTLM and SMB hardening guidance documents this as the canonical mitigation.',
    distractorRationale: {
      0: 'Disabling NTLMv1 hardens against downgrade attacks but does not stop NTLMv2 relay.',
      2: 'Disabling IPv6 limits the attack surface for mitm6 / DHCPv6 takeover but does not protect SMB once authentication is intercepted.',
      3: 'Password length is irrelevant — relay does not crack the credential, it replays the authentication directly.',
    },
    source: { name: 'Microsoft Learn — Overview of Server Message Block signing', url: 'https://learn.microsoft.com/en-us/troubleshoot/windows-server/networking/overview-server-message-block-signing' },
  },

  {
    id: 'ad-004',
    topic: 'ad',
    subtopic: 'adcs',
    difficulty: 'senior',
    question: 'In SpecterOps\' "Certified Pre-Owned" research, ESC1 describes which specific ADCS misconfiguration?',
    choices: [
      'A certificate template that allows any authenticated user to enroll AND specify an arbitrary Subject Alternative Name (SAN)',
      'A misconfigured CA that signs certificates with MD5',
      'An exposed CRL distribution point that allows public read',
      'A CA whose private key is stored on disk in plaintext',
    ],
    correctIndex: 0,
    explanation: 'ESC1 is the classic "user-supplied SAN" template misconfig: when ENROLLEE_SUPPLIES_SUBJECT is set on a template that allows client/server authentication and is enrollable by low-privilege users, an attacker can enroll with a SAN of "Administrator" and impersonate that account. Will Schroeder & Lee Christensen documented this as ESC1 in the original SpecterOps paper.',
    distractorRationale: {
      1: 'MD5-signed certificates are a separate weakness (collision risk), not the ESC1 mechanism.',
      2: 'CRL distribution exposure is an information-disclosure issue, not the ESC1 enrollment abuse.',
      3: 'Plaintext private key storage is a CA compromise pattern, not the templated enrollment misconfig that defines ESC1.',
    },
    source: { name: 'SpecterOps — Certified Pre-Owned (ADCS escalation paths)', url: 'https://posts.specterops.io/certified-pre-owned-d95910965cd2' },
  },

  {
    id: 'ad-005',
    topic: 'ad',
    subtopic: 'unconstrained-delegation',
    difficulty: 'senior',
    question: 'A workstation has "Trust this computer for delegation to any service" (unconstrained delegation) enabled. Why is this a high-impact finding?',
    choices: [
      'Because it allows the host to run software updates without admin approval',
      'Because any user authenticating to that host (including a printed-spool-coerced DC) leaves their TGT in memory, which the host\'s admin can extract and reuse',
      'Because it allows pass-the-hash from any user account on the network',
      'Because it bypasses the Kerberos pre-authentication requirement',
    ],
    correctIndex: 1,
    explanation: 'Unconstrained delegation causes the host to receive a forwardable TGT for any principal that authenticates to it. With Print Spooler / PrinterBug or similar coercion, an attacker who controls that host can capture a Domain Controller\'s TGT and impersonate it — typically leading to DA. SpecterOps and ADSecurity have written extensively on this chain.',
    distractorRationale: {
      0: 'Software updates are not gated by delegation settings; this conflates unrelated concepts.',
      2: 'Pass-the-hash is unrelated — unconstrained delegation is a Kerberos feature, not an NTLM hash issue.',
      3: 'Pre-auth bypass is the AS-REP roasting prerequisite, not what unconstrained delegation provides.',
    },
    source: { name: 'ADSecurity — Detecting Kerberoasting and Kerberos Delegation Abuse', url: 'https://adsecurity.org/?p=2293' },
  },

  {
    id: 'ad-006',
    topic: 'ad',
    subtopic: 'dcsync',
    difficulty: 'senior',
    question: 'Which two ACL rights on the domain object are the minimum required to perform a DCSync attack (replicating secrets including the krbtgt hash)?',
    choices: [
      'GenericAll on the Domain Admins group',
      'Replicating Directory Changes AND Replicating Directory Changes All',
      'Local admin on any domain-joined host',
      'WriteOwner on a Domain Controller computer object',
    ],
    correctIndex: 1,
    explanation: 'DCSync uses the legitimate MS-DRSR replication protocol (DRSGetNCChanges). The minimum required rights are "Replicating Directory Changes" (DS-Replication-Get-Changes) and "Replicating Directory Changes All" (DS-Replication-Get-Changes-All) on the domain naming context. Domain Admins, Enterprise Admins and Domain Controllers have these by default. MITRE T1003.006 documents this.',
    distractorRationale: {
      0: 'GenericAll on DA gives full takeover but is far more than the minimum — the question asks the precise replication rights.',
      2: 'Local admin on a host does not let you replicate the directory.',
      3: 'WriteOwner on a DC computer object can be a path to DA via RBCD/abuse, but it is not what DCSync requires at the protocol level.',
    },
    source: { name: 'MITRE ATT&CK T1003.006 — OS Credential Dumping: DCSync', url: 'https://attack.mitre.org/techniques/T1003/006/' },
  },

  {
    id: 'ad-007',
    topic: 'ad',
    subtopic: 'pth-vs-ptt',
    difficulty: 'senior',
    question: 'An interviewer asks the difference between Pass-the-Hash and Pass-the-Ticket. Which statement is the most accurate, senior-level distinction?',
    choices: [
      'PtH replays an NTLM hash to authenticate to NTLM-accepting services; PtT injects a Kerberos ticket (TGT or TGS) into memory to authenticate to Kerberos services',
      'PtH works only on Linux; PtT works only on Windows',
      'PtH uses the user\'s plaintext password; PtT uses a hash',
      'They are different names for the same technique',
    ],
    correctIndex: 0,
    explanation: 'Pass-the-Hash is an NTLM-protocol attack: the NTLM challenge-response is computed from the NT hash, so possessing the hash is equivalent to the password for NTLM auth. Pass-the-Ticket is a Kerberos attack: an existing TGT or TGS is loaded into memory (kerberos::ptt) and used. They target different protocols. MITRE T1550.002 (PtH) and T1550.003 (PtT) reflect this distinction.',
    distractorRationale: {
      1: 'Both attacks target Windows AD environments primarily; the OS distinction is wrong.',
      2: 'PtH never uses the plaintext password — that is the whole point. The hash is reused as-is.',
      3: 'They are distinctly named because they exploit different protocols; conflating them suggests a junior-level understanding.',
    },
    source: { name: 'MITRE ATT&CK T1550.002 — Use Alternate Authentication Material: Pass the Hash', url: 'https://attack.mitre.org/techniques/T1550/002/' },
  },

  {
    id: 'ad-008',
    topic: 'ad',
    subtopic: 'bloodhound',
    difficulty: 'senior',
    question: 'In BloodHound, which edge type indicates that the source principal can take FULL control over the destination object (read all properties, write all properties, change ACLs, reset password if a user, etc.)?',
    choices: [
      'AddMember',
      'GenericAll',
      'CanRDP',
      'ForceChangePassword',
    ],
    correctIndex: 1,
    explanation: 'GenericAll grants every Active Directory permission on the target object — equivalent to full control. BloodHound treats it as the highest-privilege ACL edge between two principals. The official BloodHound documentation lists each edge type and what attack chain it enables.',
    distractorRationale: {
      0: 'AddMember lets you add principals to a group only — narrower scope than GenericAll.',
      2: 'CanRDP is an interactive-logon right on a computer; it is not an ACL edge over an object.',
      3: 'ForceChangePassword (User-Force-Change-Password extended right) lets you reset a user\'s password without knowing the current one — powerful, but narrower than GenericAll.',
    },
    source: { name: 'BloodHound documentation — Edges (GenericAll)', url: 'https://bloodhound.specterops.io/resources/edges/generic-all' },
  },

  {
    id: 'ad-009',
    topic: 'ad',
    subtopic: 'constrained-delegation',
    difficulty: 'senior',
    question: 'A computer object is configured for "Constrained Delegation" with `msDS-AllowedToDelegateTo = CIFS/dc01.domain.local`. With control of that computer (and the s4u2self / s4u2proxy primitives), what can an attacker achieve?',
    choices: [
      'Impersonate any user (including DA) to the listed SPN, retrieving a TGS for CIFS on the DC and accessing its SMB shares — a path to DCSync',
      'Read all email from Exchange',
      'Reset every user\'s password',
      'Modify GPOs domain-wide',
    ],
    correctIndex: 0,
    explanation: 'Constrained delegation with `msDS-AllowedToDelegateTo` allows the host to request TGS-as-the-user via S4U2Self+S4U2Proxy. An attacker controlling that host can target any account (including DAs) for the listed SPN. With CIFS to a DC, the access translates to admin-level SMB and a DCSync path. Microsoft and SpecterOps document this chain.',
    distractorRationale: {
      1: 'Reading email needs Exchange-specific rights, not the listed SPN.',
      2: 'Password resets require ACL rights on the user object, not delegation.',
      3: 'GPO modification needs explicit ACL rights on the GPO/SOM, not delegation.',
    },
    source: { name: 'Microsoft Learn — Kerberos Constrained Delegation Overview', url: 'https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-server-2008-R2-and-2008/ee649281(v=ws.10)' },
  },

  {
    id: 'ad-010',
    topic: 'ad',
    subtopic: 'rbcd',
    difficulty: 'senior',
    question: 'You hold WriteDACL/GenericWrite over a computer object\'s `msDS-AllowedToActOnBehalfOfOtherIdentity` attribute. What attack does this enable, and what makes it powerful?',
    choices: [
      'Resource-Based Constrained Delegation (RBCD) abuse: the attacker writes their own controlled-account SID into the attribute, then uses S4U2Self+S4U2Proxy to obtain a TGS as any user (including DA) for any service on the target — turns "write one attribute" into full host takeover',
      'Pass-the-Hash with the krbtgt key',
      'Kerberoasting against a Domain Admin SPN',
      'NetBIOS poisoning',
    ],
    correctIndex: 0,
    explanation: 'RBCD configuration is stored on the resource (target) computer object. WriteDACL/GenericWrite on the target lets the attacker add an attacker-controlled principal (often a fake computer object created via MachineAccountQuota) to `msDS-AllowedToActOnBehalfOfOtherIdentity`. With Rubeus or Impacket, this becomes a TGS impersonation against the target. Elad Shamir\'s "Wagging the Dog" research is the canonical reference.',
    distractorRationale: {
      1: 'PtH with krbtgt is the Golden Ticket flow, separate.',
      2: 'Kerberoasting needs an SPN and offline cracking; this attack is online and uses delegation.',
      3: 'NetBIOS poisoning is a name-resolution attack, unrelated.',
    },
    source: { name: 'SpecterOps / Elad Shamir — Wagging the Dog: Abusing Resource-Based Constrained Delegation', url: 'https://shenaniganslabs.io/2019/01/28/Wagging-the-Dog.html' },
  },

  {
    id: 'ad-011',
    topic: 'ad',
    subtopic: 'silver-ticket',
    difficulty: 'senior',
    question: 'A Silver Ticket attack requires which key, and what does it let an attacker do?',
    choices: [
      'The krbtgt account hash; it forges a TGT that grants any access',
      'The TARGET service account\'s NTLM hash (or AES key); it forges a TGS that authenticates to that one service as any user (often as DA), without ever talking to the KDC',
      'A Domain Admin\'s plaintext password; it grants Enterprise Admin globally',
      'The target machine\'s BIOS password',
    ],
    correctIndex: 1,
    explanation: 'A Silver Ticket is a forged TGS encrypted with the service account key. It is narrower than a Golden Ticket (which uses krbtgt and forges TGTs), but it skips the KDC entirely — making it stealthier in many detections. ADSecurity and MITRE T1558.002 document the technique.',
    distractorRationale: {
      0: 'krbtgt hash is for Golden Tickets, not Silver.',
      2: 'A plaintext DA password is more powerful but unrelated to ticket forgery mechanics.',
      3: 'BIOS passwords are not in the AD trust chain at all.',
    },
    source: { name: 'MITRE ATT&CK T1558.002 — Silver Ticket', url: 'https://attack.mitre.org/techniques/T1558/002/' },
  },

  {
    id: 'ad-012',
    topic: 'ad',
    subtopic: 'golden-ticket',
    difficulty: 'senior',
    question: 'A Golden Ticket attack uses the KRBTGT account hash. From a defender\'s perspective, what is the recommended remediation after a Golden Ticket compromise is suspected?',
    choices: [
      'Disable LLMNR',
      'Reset the KRBTGT password TWICE (with the recommended waiting interval) to invalidate any forged TGTs in flight',
      'Reboot all domain controllers',
      'Block port 445 at the firewall',
    ],
    correctIndex: 1,
    explanation: 'TGTs are encrypted with the krbtgt key. The current and previous keys are accepted (to allow rotation without service disruption), so a single reset still allows an attacker holding the prior key to operate. Two resets, with the proper interval, evict any forged tickets. Microsoft\'s incident-response guidance documents this exactly.',
    distractorRationale: {
      0: 'LLMNR is unrelated to ticket forgery.',
      2: 'Rebooting DCs does not invalidate an attacker-forged TGT signed with the old krbtgt hash.',
      3: 'Blocking SMB does not stop Kerberos auth.',
    },
    source: { name: 'Microsoft Learn — KRBTGT account password reset scripts (Mitigate Golden Tickets)', url: 'https://learn.microsoft.com/en-us/defender-for-identity/security-assessment-reset-krbtgt' },
  },

  {
    id: 'ad-013',
    topic: 'ad',
    subtopic: 'printnightmare',
    difficulty: 'senior',
    question: 'PrintNightmare (CVE-2021-1675 / CVE-2021-34527) abuses what Windows feature to achieve SYSTEM-level RCE, and why was it especially dangerous in domain environments?',
    choices: [
      'The Windows Print Spooler service: a low-privileged authenticated user can call RpcAddPrinterDriverEx (locally or over RPC) to load an attacker-supplied DLL into spoolsv.exe (running as SYSTEM). On a Domain Controller, this is direct SYSTEM on the DC — full domain compromise.',
      'The Windows Defender service',
      'The Server (LanmanServer) service',
      'The Telnet service',
    ],
    correctIndex: 0,
    explanation: 'PrintNightmare exploited the Print Spooler\'s Point and Print and driver loading paths. Because spoolsv.exe runs as SYSTEM and is enabled by default on Domain Controllers, a domain user could attain SYSTEM on the DC. Microsoft\'s advisory and CISA bulletin both detail the technique.',
    distractorRationale: {
      1: 'Defender vulnerabilities are different CVEs.',
      2: 'LanmanServer is involved in SMB attacks, not print.',
      3: 'Telnet is not the affected service.',
    },
    source: { name: 'Microsoft MSRC — CVE-2021-34527 (Windows Print Spooler RCE)', url: 'https://msrc.microsoft.com/update-guide/vulnerability/CVE-2021-34527' },
  },

  {
    id: 'ad-014',
    topic: 'ad',
    subtopic: 'nopac',
    difficulty: 'senior',
    question: 'noPac (CVE-2021-42278 / CVE-2021-42287) chains two CVEs to escalate from any low-priv domain user to Domain Admin. What is the attack flow?',
    choices: [
      'Create a computer account (default MachineAccountQuota=10), rename it to a DC name (e.g., DC$ → DC), request a TGT, rename back, then request an S4U2Self ticket — the KDC returns a PAC for an Administrator due to the SamAccountName confusion (42278) and improper PAC validation (42287)',
      'Disable the Domain Admins group',
      'Modify the Default Domain Policy via SMB share',
      'Brute force the krbtgt password',
    ],
    correctIndex: 0,
    explanation: 'noPac (sometimes "sAMAccountName Spoofing") combines CVE-2021-42278 (insufficient sAMAccountName validation when creating machine accounts) and CVE-2021-42287 (KDC improperly validates PAC against renamed accounts). The chain requires only an unprivileged user with default MAQ. Microsoft\'s patch advisories and tools like noPac.py / Impacket\'s scripts implement the exploit.',
    distractorRationale: {
      1: 'Disabling DA does nothing for unauth attackers and is not the chain.',
      2: 'GPO modification needs ACL rights on the policy and is a separate path.',
      3: 'Brute-forcing krbtgt is computationally infeasible.',
    },
    source: { name: 'Microsoft Security — CVE-2021-42278 / CVE-2021-42287 (sAMAccountName spoofing)', url: 'https://msrc.microsoft.com/update-guide/vulnerability/CVE-2021-42278' },
  },

  {
    id: 'ad-015',
    topic: 'ad',
    subtopic: 'gpp-cpassword',
    difficulty: 'senior',
    question: 'You find an XML file in `\\\\domain\\SYSVOL\\domain\\Policies\\<GUID>\\Machine\\Preferences\\Groups\\Groups.xml` containing `cpassword="..."`. What is this and why is it interesting?',
    choices: [
      'Group Policy Preferences (GPP) cpassword: AES-encrypted with a published static key — Microsoft documented the key publicly, so any authenticated user who can read SYSVOL can decrypt these to plaintext. Even after the 2014 patch (MS14-025), pre-existing cpassword values often linger',
      'A salted bcrypt hash that is uncrackable',
      'A unique per-host secret',
      'A self-signed certificate',
    ],
    correctIndex: 0,
    explanation: 'GPP allowed admins to push local accounts/passwords via GPO, encrypted with a fixed AES-256 key whose value Microsoft published. Tools like gpp-decrypt and Get-GPPPassword still find legacy artefacts in many domains. ADSecurity has a write-up; Microsoft\'s MS14-025 advisory describes the deprecation.',
    distractorRationale: {
      1: 'It is reversible AES with a public key, not a hash.',
      2: 'The key is the same domain-wide, not per-host.',
      3: 'It is encrypted password material, not a certificate.',
    },
    source: { name: 'Microsoft MS14-025 — Vulnerability in Group Policy Preferences', url: 'https://learn.microsoft.com/en-us/security-updates/securitybulletins/2014/ms14-025' },
  },

  {
    id: 'ad-016',
    topic: 'ad',
    subtopic: 'adcs-esc8',
    difficulty: 'senior',
    question: 'ADCS ESC8 is exploited by combining which two primitives?',
    choices: [
      'NTLM relay (e.g., from PetitPotam coercion) targeting the AD CS HTTP Web Enrollment endpoint, which lacks Extended Protection for Authentication (EPA) by default, allowing the relayed authentication to enroll a certificate as the coerced principal — typically a Domain Controller, leading to DA',
      'Kerberoasting + Pass-the-Hash',
      'ARP spoofing + DNS poisoning',
      'GPP cpassword + Pass-the-Ticket',
    ],
    correctIndex: 0,
    explanation: 'ESC8 is the relay path: the AD CS Web Enrollment endpoint accepts NTLM and historically did not enforce EPA. Combined with a forced authentication primitive (PetitPotam, PrinterBug), an attacker relays a DC\'s authentication and obtains a machine certificate, then authenticates as the DC. SpecterOps documented ESC1–ESC8 in the Certified Pre-Owned paper.',
    distractorRationale: {
      1: 'Kerberoasting+PtH is a different chain.',
      2: 'L2 attacks are not the relay vector here.',
      3: 'GPP cpassword + PtT is a separate path.',
    },
    source: { name: 'SpecterOps — Certified Pre-Owned (ESC8 details)', url: 'https://posts.specterops.io/certified-pre-owned-d95910965cd2' },
  },

  {
    id: 'ad-017',
    topic: 'ad',
    subtopic: 'sysvol-creds',
    difficulty: 'senior',
    question: 'Beyond GPP cpassword, what is another classic source of cleartext or recoverable credentials in SYSVOL that a senior pentester checks?',
    choices: [
      'Logon scripts (.bat, .vbs, .ps1) that hardcode service account passwords for net use / mapped drives — readable by every authenticated user',
      'The DNS database',
      'The schema partition',
      'The recycle bin',
    ],
    correctIndex: 0,
    explanation: 'SYSVOL is world-readable to authenticated users, and admins still write logon/startup scripts that bake in service-account credentials for "convenience". Tools like PowerView (Find-DomainShare with grep) and the BloodHound NetExec collection strategy surface these. ADSecurity has called this out for years.',
    distractorRationale: {
      1: 'DNS records are not in SYSVOL.',
      2: 'Schema partition holds class definitions, no credentials.',
      3: 'There is no AD recycle bin in SYSVOL with credentials.',
    },
    source: { name: 'ADSecurity — Mining for credentials in SYSVOL', url: 'https://adsecurity.org/?p=2288' },
  },

  {
    id: 'ad-018',
    topic: 'ad',
    subtopic: 'potato',
    difficulty: 'senior',
    question: 'You have a foothold as a service account on Windows that holds SeImpersonatePrivilege but not SYSTEM. Which family of exploits is the canonical local privilege escalation, and what privilege constant do they all rely on?',
    choices: [
      'The "Potato" family (Hot Potato, Rotten Potato, Juicy Potato, RoguePotato, PrintSpoofer, GodPotato) — they coerce a privileged token via NTLM/RPC and impersonate it. They depend on SeImpersonatePrivilege, which Windows grants to many service accounts (IIS, SQL).',
      'BloodHound\'s GenericAll',
      'PrintNightmare',
      'Kerberoasting',
    ],
    correctIndex: 0,
    explanation: 'SeImpersonatePrivilege ("Impersonate a client after authentication") is the gatekeeper for the Potato chain. Each variant differs in how it triggers the privileged auth (COM, DCOM, named-pipe spool, RPC) but they all then call ImpersonateLoggedOnUser. Microsoft documents the privilege; SpecterOps and many writeups explain the chain.',
    distractorRationale: {
      1: 'GenericAll is an AD ACL, not a Windows privilege constant.',
      2: 'PrintNightmare is a separate spooler RCE, not a token impersonation primitive.',
      3: 'Kerberoasting is offline TGS cracking.',
    },
    source: { name: 'Microsoft Learn — Impersonate a client after authentication (SeImpersonatePrivilege)', url: 'https://learn.microsoft.com/en-us/windows/security/threat-protection/security-policy-settings/impersonate-a-client-after-authentication' },
  },

  {
    id: 'ad-019',
    topic: 'ad',
    subtopic: 'laps',
    difficulty: 'senior',
    question: 'LAPS (Local Administrator Password Solution / Windows LAPS) does what, and where does it store the password?',
    choices: [
      'It generates a unique random local administrator password per machine and stores it in an AD attribute (`ms-Mcs-AdmPwd` for legacy LAPS, `msLAPS-Password` for Windows LAPS) on the computer object — readable only by accounts granted "All Extended Rights" (or specific LAPS-read rights)',
      'It stores all local passwords in C:\\\\Windows\\\\SAM',
      'It uses TPM-only storage that cannot be read by anyone',
      'It posts passwords to a web service',
    ],
    correctIndex: 0,
    explanation: 'LAPS rotates local admin passwords per machine and stores them in AD on the computer object\'s attribute. Read access is controlled by ACLs. Misconfigured ACLs (e.g., Helpdesk OU with "All Extended Rights" on too broad a scope) are a common senior-level finding. Microsoft\'s LAPS documentation describes the model.',
    distractorRationale: {
      1: 'SAM stores hashes locally, not LAPS passwords.',
      2: 'TPM is not used for LAPS storage.',
      3: 'No external service is involved.',
    },
    source: { name: 'Microsoft Learn — Windows LAPS overview', url: 'https://learn.microsoft.com/en-us/windows-server/identity/laps/laps-overview' },
  },

  {
    id: 'ad-020',
    topic: 'ad',
    subtopic: 'gmsa',
    difficulty: 'senior',
    question: 'Group Managed Service Accounts (gMSA) store their password in AD encrypted with what key, and what is the implication for an attacker who controls a host with read rights to `msDS-ManagedPassword`?',
    choices: [
      'The password blob is retrievable as cleartext by any principal in `msDS-GroupMSAMembership`. With the blob, tools like gMSADumper or Impacket\'s `getMSACreds` derive the NT hash and any AES keys directly — no cracking needed',
      'It is encrypted with the user\'s biometric data',
      'It is stored in the BIOS',
      'It is never accessible at all',
    ],
    correctIndex: 0,
    explanation: 'gMSA passwords are returned as a structured blob (ms-DS-ManagedPassword) to authorised principals. The blob contains the current and previous passwords from which NT hashes and Kerberos keys can be derived deterministically — which means controlling a host that is allowed to read it is equivalent to having the credential. SpecterOps and Microsoft\'s gMSA docs both describe this.',
    distractorRationale: {
      1: 'gMSA does not use biometrics.',
      2: 'BIOS storage is unrelated.',
      3: 'It is accessible by design to authorised principals.',
    },
    source: { name: 'Microsoft Learn — Group Managed Service Accounts', url: 'https://learn.microsoft.com/en-us/windows-server/security/group-managed-service-accounts/group-managed-service-accounts-overview' },
  },

  {
    id: 'ad-021',
    topic: 'ad',
    subtopic: 'adminsdholder',
    difficulty: 'senior',
    question: 'AdminSDHolder is which AD object, and why does it matter to a senior pentester?',
    choices: [
      'A protected container whose ACL is copied (every 60 minutes by SDProp) onto every "protected" admin object (Domain Admins, Enterprise Admins, etc.). Modifying AdminSDHolder\'s ACL is a stealthy persistence mechanism — your changes propagate to all protected groups domain-wide',
      'A user account that owns DNS records',
      'A Group Policy that disables passwords',
      'A scheduled task that backs up SYSVOL',
    ],
    correctIndex: 0,
    explanation: 'SDProp uses AdminSDHolder as the template for protected admin ACLs. An attacker with the right to modify AdminSDHolder achieves persistent privileged access across every protected group, even if individual ACLs are reverted. ADSecurity / Sean Metcalf has documented this as a defender concern and an attacker tradecraft.',
    distractorRationale: {
      1: 'It is not a DNS owner.',
      2: 'It is not a GPO.',
      3: 'It is not a scheduled task.',
    },
    source: { name: 'Microsoft Learn — Appendix C: Protected Accounts and Groups (AdminSDHolder)', url: 'https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/plan/security-best-practices/appendix-c--protected-accounts-and-groups-in-active-directory' },
  },

  {
    id: 'ad-022',
    topic: 'ad',
    subtopic: 'sid-history',
    difficulty: 'senior',
    question: 'In the SID History attack, what is being abused?',
    choices: [
      'The `SIDHistory` attribute (intended to support migrations across domains) accumulates SIDs that grant access in legacy domains; an attacker who can write SIDHistory can append a privileged SID (e.g., DA of another trusted domain) and inherit those rights. Forged Golden Tickets often include SIDHistory for stealthy cross-domain compromise.',
      'The local SAM database',
      'The DNS root hints',
      'The browser history of the DA',
    ],
    correctIndex: 0,
    explanation: 'SIDHistory is the legitimate attribute that lets a migrated user retain access in their old domain. It also makes Golden Tickets dangerous across forest trusts when SID Filtering is misconfigured. Microsoft\'s docs and Sean Metcalf\'s presentations describe the attack surface and the Quarantine bit on trusts that should mitigate it.',
    distractorRationale: {
      1: 'SAM is local and unrelated to the attribute.',
      2: 'DNS root hints are infrastructure config.',
      3: 'Browser history has no security role here.',
    },
    source: { name: 'Microsoft Learn — Security considerations for trusts (SID Filtering)', url: 'https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/manage/component-updates/sid-filtering-quarantining' },
  },

  {
    id: 'ad-023',
    topic: 'ad',
    subtopic: 'forest-trust',
    difficulty: 'senior',
    question: 'In a forest trust where SID filtering is correctly enforced (Quarantine bit set), can a Golden Ticket forged in domain A include SID History to compromise domain B?',
    choices: [
      'Yes, regardless of SID filtering',
      'No — SID filtering at the forest boundary strips foreign SIDs from incoming TGTs, neutralising the SID-History portion of cross-forest Golden Tickets. SID filtering is the senior-level defence to verify in scoping',
      'Only on Sundays',
      'Only if NTLMv1 is enabled',
    ],
    correctIndex: 1,
    explanation: 'When SID filtering / Quarantine is enabled on the trust, foreign SIDs in tickets are filtered as the ticket crosses the trust boundary. This is why forest is the security boundary, not domain — but only when filtering is correctly configured. Misconfigured trusts (especially long-lived ones from migrations) are common findings.',
    distractorRationale: {
      0: 'The whole point of SID filtering is to prevent this.',
      2: 'Day of week is irrelevant.',
      3: 'NTLMv1 is unrelated.',
    },
    source: { name: 'Microsoft Learn — SID Filtering and Forest Trusts', url: 'https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/manage/component-updates/sid-filtering-quarantining' },
  },

  {
    id: 'ad-024',
    topic: 'ad',
    subtopic: 'rodc',
    difficulty: 'senior',
    question: 'A Read-Only Domain Controller (RODC) caches credentials only for accounts in its `Password Replication Policy`. From an attacker\'s perspective, why is an RODC still a useful target?',
    choices: [
      'If you compromise the RODC, you obtain hashes for every account it has cached (typically branch-office users) — and any account whose `userAccountControl` allows replication to it; you can also use the RODC\'s krbtgt key to forge TGTs valid for that RODC\'s scope',
      'RODCs grant Domain Admin to anyone who logs in',
      'RODCs disable LDAP signing',
      'RODCs run on Linux',
    ],
    correctIndex: 0,
    explanation: 'An RODC holds a separate krbtgt key (krbtgt_<RODC#>) and only caches credentials of allowed accounts. Compromising the RODC yields those cached hashes and the RODC-scoped krbtgt — useful for lateral movement and (with poor PRP design) sometimes a stepping stone. Microsoft\'s RODC docs describe the model.',
    distractorRationale: {
      1: 'RODCs do not grant DA on logon.',
      2: 'LDAP signing is not auto-disabled by RODC.',
      3: 'RODCs run on Windows Server.',
    },
    source: { name: 'Microsoft Learn — Read-Only Domain Controllers', url: 'https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/deploy/rodc/install-a-windows-server-2012-active-directory-read-only-domain-controller--level-200-' },
  },

  {
    id: 'ad-025',
    topic: 'ad',
    subtopic: 'lsass-protections',
    difficulty: 'senior',
    question: 'Which Windows control most directly mitigates an attacker dumping credentials from LSASS memory (e.g., via mimikatz `sekurlsa::logonpasswords`)?',
    choices: [
      'Disabling SMBv1',
      'Enabling Credential Guard (VBS-isolated LSAIso) AND configuring LSA Protection (RunAsPPL) — both prevent or significantly raise the bar for in-memory credential extraction',
      'Setting a longer minimum password length',
      'Enabling IPSec',
    ],
    correctIndex: 1,
    explanation: 'Credential Guard moves LSASS secrets into a virtualisation-isolated process (LSAIso), and LSA Protection makes lsass.exe a Protected Process Light (PPL), blocking unprivileged drivers/tools from opening it. Microsoft\'s Credential Guard and LSA Protection docs cover both controls and their bypass considerations.',
    distractorRationale: {
      0: 'SMBv1 is unrelated to LSASS dumping.',
      2: 'Password length does not stop in-memory dumping of plaintext/credentials.',
      3: 'IPSec is a network control.',
    },
    source: { name: 'Microsoft Learn — Configure Credential Guard', url: 'https://learn.microsoft.com/en-us/windows/security/identity-protection/credential-guard/' },
  },
];
